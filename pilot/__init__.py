"""Pilot corpus: seeded stratified sampling + disjoint splits.

Reuses the smoke collection engine (sources/, filters.py, fetcher.py).
This package adds ONLY the pilot-specific layer: sampling strategy,
splits, and the 2-rater self-containedness workflow.
"""
__version__ = "0.1.0"
